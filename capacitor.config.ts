import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.polodev.garciaEpp',
  appName: 'SSOMA Constructora García',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
